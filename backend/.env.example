# Existing keys...
GEMINI_API_KEY=__your_key__
DRY_RUN=true

# Firebase Admin
FIREBASE_PROJECT_ID=__REPLACE_ME__
# Option A (recommended for Vercel): base64 of the entire service account JSON
FIREBASE_SERVICE_ACCOUNT_BASE64=__REPLACE_ME__
# Option B (ADC fallback): provide a file path in your environment
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccount.json

# CORS
ALLOWED_ORIGINS=http://localhost:5173,https://your-production-frontend.com
